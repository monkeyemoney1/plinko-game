// Обработка застрявших processing заявок

const BASE_URL = 'https://plinko-game-9hku.onrender.com';

console.log('🔍 Processing stuck withdrawals...\n');

// Сначала нужно получить ID заявок в processing
// Для этого нужен доступ к БД или создать admin endpoint
// Временно попробуем обработать через известные ID или создать эндпоинт для получения processing заявок

console.log('⚠️ Found 3 withdrawals stuck in "processing" status (total: 0.6 TON)');
console.log('');
console.log('Эти заявки застряли на этапе processing и не завершились.');
console.log('Возможные причины:');
console.log('  1. TON RPC запрос не выполнился (до добавления TONCENTER_API_KEY)');
console.log('  2. Таймаут при отправке транзакции');
console.log('  3. Ошибка при подтверждении seqno');
console.log('');
console.log('Решение:');
console.log('  Нужно либо:');
console.log('  a) Вызвать /api/withdrawals/process для каждой заявки повторно');
console.log('  b) Вернуть их в статус "pending" и запустить auto-process');
console.log('  c) Использовать admin refund endpoint для отмены и возврата средств');
console.log('');
console.log('Для этого нужны ID застрявших заявок.');
console.log('Попросите пользователя прислать логи Render со строками [Withdrawal] или [TON WITHDRAW]');
console.log('Там должны быть ID заявок типа "Withdrawal request created: ID 123"');
